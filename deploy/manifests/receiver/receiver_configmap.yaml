apiVersion: v1
kind: ConfigMap
metadata:
  name: l4s-lidar-receiver-config
  namespace: l4s-lidar
  labels:
    app.kubernetes.io/name: l4s-lidar-receiver
data:
  LOG_LEVEL: "info"
  DEFAULT_PORT: "30112"
  DEFAULT_FRAME_ID: "husky/os_sensor"
  NOTE: "All runtime parameters may be overridden dynamically by run_k8s.sh"
